# Generate a secret (run once, save the output):
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# Optional — defaults shown
# PORT=3000
# HOST=0.0.0.0
# JWT_EXPIRES=7d
# NODE_ENV=production
